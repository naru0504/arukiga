{
  "name": "arukiga",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.14.1"
  },
  "scripts": {
    "tsc": "tsc -b packages/front/tsconfig.json",
    "tsc-w": "yarn tsc -w",
    "build": "lerna run build --stream",
    "build:prod": "lerna run build:prod --stream",
    "test": "lerna run test --stream",
    "deploy": "lerna run deploy"
  }
}
